# LAB-26 · 폴링의 은퇴식 (정점 실습)
제공: 벡터 트램펄린 복사·IRQ 모드 스택·껍데기(irq_wrap)·VIC/타이머 초기화 — TODO는 핸들러 본문뿐.
- `make check` : 골격은 진행 불가(미클리어 재진입)로 타임아웃 FAIL — 그 자체가 출발선 증거.
                 본문(판별→INTCLR→ticks++) 완성 후 IRQ x5 OK → echo $? = 0
- 위반 실험 A(2조): INTCLR 주석 → 관측 기록(폭풍/정지 등 환경별 상이 — 다른 관측도 유효) 후 원복
- 위반 실험 B(3조): ticks의 volatile 제거 + -O2 재빌드 → 메인 무한 대기 확인 후 원복
- 세 문 진단: 장치 IE(bit5) → VIC EN(bit4) → CPSR I — "신호가 안 온다"의 점검 순서
강사용: irq26_sol.c는 학생 배포본에서 제거한다.
